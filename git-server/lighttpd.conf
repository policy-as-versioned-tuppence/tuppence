# Smart-HTTP git for Flux's source-controller. Read-only clone, no auth:
# the served tree is the public demo gitops repo, inside the KinD cluster only.
server.modules = ("mod_alias", "mod_cgi", "mod_setenv")
server.document-root = "/www"
server.port = 8080
$HTTP["url"] =~ "^/cgi-bin/git" {
    alias.url = ("/cgi-bin/git" => "/usr/lib/git-core/git-http-backend")
    cgi.assign = ("" => "")
    setenv.add-environment = (
        "GIT_PROJECT_ROOT" => "/srv/git",
        "GIT_HTTP_EXPORT_ALL" => "1",
    )
}
